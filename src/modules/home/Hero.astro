---
/**
 * Hero — Introducción Editorial
 *
 * Sección principal con tipografía monumental y animaciones glaciales.
 * Background: Fotografía arquitectónica con overlay oscuro para textura.
 */
---

<section id="hero" class="hero-section">
  <!-- Background Image -->
  <div class="hero-background"></div>

  <!-- Dark Overlay -->
  <div class="hero-overlay"></div>

  <!-- Content (sobre el overlay) -->
  <div class="container-arkeon hero-container">
    <div class="hero-content">
      <!-- Título Principal -->
      <h1 class="hero-title animate-fade-in">
        ATELIER<br />VIREN
      </h1>

      <!-- Subtítulo Conceptual -->
      <p class="hero-subtitle animate-fade-in" style="animation-delay: 400ms;">
        Intervenciones contemporáneas<br />sobre arquitectura histórica
      </p>

      <!-- Manifiesto -->
      <div
        class="hero-manifesto animate-fade-in-up"
        style="animation-delay: 800ms;"
      >
        <p class="font-serif">
          El patrimonio arquitectónico es memoria física de nuestra historia.
          Nuestra labor no es construir lo nuevo, sino preservar lo existente
          con rigor técnico y respeto histórico.
        </p>
      </div>

      <!-- Scroll Indicator -->
      <div
        class="scroll-indicator animate-fade-in"
        style="animation-delay: 1400ms;"
      >
        <div class="scroll-line"></div>
        <span class="font-mono">SCROLL</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    padding-top: var(--spacing-2xl);
    padding-bottom: var(--spacing-3xl);
    overflow: hidden;
  }

  /* Background Image - Escalera del Palacio Municipal */
  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("/images/palacio/03.png");
    background-size: cover;
    background-position: center;
    filter: grayscale(100%);
    z-index: 0;
  }

  /* Dark Overlay - 85% opacidad para legibilidad */
  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      to bottom,
      rgba(47, 47, 45, 0.88) 0%,
      rgba(47, 47, 45, 0.85) 70%,
      rgba(47, 47, 45, 0.92) 100%
    );
    z-index: 1;
  }

  /* Container con z-index para contenido */
  .hero-container {
    position: relative;
    z-index: 2;
  }

  .hero-content {
    max-width: var(--max-width-content);
    margin: 0 auto;
  }

  /* Título Principal - Ahora en color claro para contraste */
  .hero-title {
    font-family: var(--font-family-serif);
    font-size: clamp(3.5rem, 8vw, 7rem);
    font-weight: var(--font-weight-bold);
    line-height: 0.95;
    letter-spacing: -0.03em;
    color: var(--color-stone-light); /* Cambio a claro */
    margin-bottom: var(--spacing-lg);
    animation-duration: var(--duration-glacial);
    animation-fill-mode: both;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3); /* Sutil shadow para profundidad */
  }

  /* Subtítulo Conceptual */
  .hero-subtitle {
    font-family: var(--font-family-sans);
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    font-weight: var(--font-weight-normal);
    line-height: 1.4;
    color: rgba(231, 229, 225, 0.8); /* Stone light con transparencia */
    margin-bottom: var(--spacing-2xl);
    max-width: 600px;
    animation-duration: var(--duration-glacial);
    animation-fill-mode: both;
  }

  /* Manifiesto */
  .hero-manifesto {
    max-width: var(--max-width-text);
    margin-bottom: var(--spacing-3xl);
    padding-left: var(--spacing-md);
    border-left: 2px solid rgba(231, 229, 225, 0.3);
    animation-duration: var(--duration-slow);
    animation-fill-mode: both;
  }

  .hero-manifesto p {
    font-size: clamp(1rem, 1.5vw, 1.25rem);
    line-height: 1.7;
    color: rgba(231, 229, 225, 0.85);
  }

  /* Scroll Indicator */
  .scroll-indicator {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    color: rgba(231, 229, 225, 0.6);
    animation-duration: var(--duration-base);
    animation-fill-mode: both;
  }

  .scroll-line {
    width: 48px;
    height: 1px;
    background-color: rgba(231, 229, 225, 0.4);
    animation: scroll-pulse 2s ease-in-out infinite;
  }

  .scroll-indicator span {
    font-size: 0.75rem;
    letter-spacing: 0.15em;
  }

  @keyframes scroll-pulse {
    0%,
    100% {
      opacity: 0.3;
      transform: scaleX(1);
    }
    50% {
      opacity: 1;
      transform: scaleX(1.5);
    }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-section {
      min-height: 90vh;
      padding-top: var(--spacing-xl);
      padding-bottom: var(--spacing-2xl);
    }

    .hero-background {
      background-position: 60% center; /* Mejor encuadre en mobile */
    }

    .hero-title {
      margin-bottom: var(--spacing-md);
    }

    .hero-subtitle {
      margin-bottom: var(--spacing-xl);
    }

    .hero-manifesto {
      margin-bottom: var(--spacing-2xl);
      padding-left: var(--spacing-sm);
    }
  }

  /* Composición Asimétrica */
  @media (min-width: 1024px) {
    .hero-content {
      padding-left: 5%;
    }

    .hero-title {
      margin-left: -0.05em; /* Optical alignment */
    }
  }
</style>
